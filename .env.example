# Copy this file to .env on the Pi and set real values.
# .env should NEVER be committed to git (it's in .gitignore).

DASH_USER=admin
DASH_PASSWORD=change-this-to-something-strong

# Wake-on-LAN: MAC address of the machine to wake (the adapter it sleeps on)
WOL_TARGET_MAC=AA:BB:CC:DD:EE:FF
